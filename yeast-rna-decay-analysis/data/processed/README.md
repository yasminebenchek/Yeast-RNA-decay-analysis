# Processed data

This folder may contain derived, non-raw outputs that are safe to share.

`Decay_Half_Lives.csv` is a processed half-life results table used as an example input for downstream summary/visualisation scripts.
